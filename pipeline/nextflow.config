process { 
    executor = 'awsbatch' 
    queueSize = 100
    errorStrategy = 'retry'
    maxRetries = 20
    maxErrors = 100
}

process.resourceLabels = ['allen-batch-pipeline': 'aind-ephys-pipeline-kilosort25']
